#!/usr/bin/env node

const { createUniversalCommand } = require("../lib/core/universal-command");

// Create and run the format command
const formatCommand = createUniversalCommand("format");
formatCommand();
