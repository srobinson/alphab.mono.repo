#!/usr/bin/env node

const { createUniversalCommand } = require("../lib/core/universal-command");

// Create and run the typeCheck command
const typeCheckCommand = createUniversalCommand("typeCheck");
typeCheckCommand();
