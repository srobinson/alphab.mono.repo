#!/usr/bin/env node

const { createUniversalCommand } = require("../lib/core/universal-command");

// Create and run the test command
const testCommand = createUniversalCommand("test");
testCommand();
