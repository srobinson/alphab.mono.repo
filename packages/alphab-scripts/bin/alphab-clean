#!/usr/bin/env node

const { createUniversalCommand } = require("../lib/core/universal-command");

// Create and run the clean command
const cleanCommand = createUniversalCommand("clean");
cleanCommand();
