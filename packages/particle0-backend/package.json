{
  "name": "particle0-backend",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev": "poetry run uvicorn particle0_backend.main:app --reload --port 8000",
    "build": "poetry build",
    "lint": "flake8",
    "format": "black .",
    "test": "python -m pytest"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
