{
  "name": "@alphab/logging",
  "version": "0.1.0",
  "description": "Reusable logging package with structured logging, multiple handlers, and easy configuration",
  "private": true,
  "scripts": {
    "bootstrap": "rm -rf .venv poetry.lock && python -m venv .venv && source .venv/bin/activate && poetry lock && poetry install",
    "build": "poetry build",
    "lint": "flake8 . --count --show-source --statistics && mypy alphab_logging",
    "format": "black .",
    "test": "python -m pytest",
    "test:coverage": "python -m pytest --cov=alphab_logging --cov-report=html --cov-report=term"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
